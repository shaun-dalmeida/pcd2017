#include<stdio.h>
#include<math.h>
int main()
{
	double num,result;
	printf("Enter the number: \n");
	scanf("%lf",&num);
	result=sqrt(num);
	printf("Square root of the given number: %lf",result);
	return 0;
}
